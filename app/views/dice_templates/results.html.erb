<h1><%= @number_of_dice %>d<%= @how_many_sides %></h1>

<a href="/process_roll?dice=<%= @number_of_dice %>&sides=<%= @how_many_sides %>">Roll again</a>

|

<a href="/">Go back home</a>

<ol>
  <% @rolls.each do |a_roll| %>
    <li>
      <em>Result:</em> <strong><%= a_roll %></strong>
    </li>
  <% end %>
</ol>
